package TowerDefense.Entity.Enemy;


import javafx.scene.image.Image;

public class BossEnemy extends Enemy {
    public BossEnemy(double x, double y, double health, double reward, double speed, double armor, Image img) {
        super(x, y, health, reward, speed, armor, img);
    }
}
